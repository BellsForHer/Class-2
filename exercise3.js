// arrayOfMultiples(7, 5) 

function getMultiples(integer, limit) {
  let multiples = [];
  
  for(let i = integer; i<= limit; i= i+integer){
    multiples.push(i);
  }
  return multiples;
}
getMultiples(7, 35); 

function getMultiples(integer, limit) {
  let multiples = [];
  
  for(let i = integer; i<= limit; i= i+integer){
    multiples.push(i);
  }
  return multiples;
}
getMultiples(12, 120);

function getMultiples(integer, limit) {
  let multiples = [];
  
  for(let i = integer; i<= limit; i= i+integer){
    multiples.push(i);
  }
  return multiples;
}
getMultiples(17, 102); 